/**
 * @deprecated Import from '@common/import' instead.
 * Kept for backward compatibility with existing imports.
 */
export {
  detectFormat,
  createParser,
  type ImportFileFormat,
} from '../../common/import/import-parser.factory';
